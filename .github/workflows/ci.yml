on:
  push:

jobs:
  build-release:
    runs-on: windows-latest

    env:
      MIX_ENV: prod

    steps:
    - uses: actions/checkout@v4

    - name: Set up Elixir
      uses: erlef/setup-beam@v1
      with:
        version-file: .tool-versions
        version-type: strict
